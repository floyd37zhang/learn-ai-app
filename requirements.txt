# ---------- 核心依赖 ----------
openai>=1.30.0            # LLM 调用（OpenAI 兼容接口）
psycopg[binary]>=3.1.0    # PostgreSQL 驱动
PyMuPDF>=1.24.0           # PDF 文本层提取 / 渲染
python-docx>=1.1.0        # Word 解析
pydantic>=2.7.0           # 数据校验（预留）

# ---------- OCR（扫描版 PDF / 图片才需要，可后装） ----------
# paddlepaddle>=2.6.0
# paddleocr>=2.7.0

# ---------- 向量化（可选，用于语义检索；先不装也能入库） ----------
# sentence-transformers>=3.0.0
